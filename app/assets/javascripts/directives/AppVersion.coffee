directivesModule.directive('appVersion', ['version', (version) ->
        (scope, elm, attrs) ->
            elm.text(version);
    ])
directivesModule2.directive('appVersion', ['version', (version) ->
  (scope, elm, attrs) ->
    elm.text(version);
])
directivesModule3.directive('appVersion', ['version', (version) ->
  (scope, elm, attrs) ->
    elm.text(version);
])
directivesModule4.directive('appVersion', ['version', (version) ->
  (scope, elm, attrs) ->
    elm.text(version);
])
directivesModule5.directive('appVersion', ['version', (version) ->
  (scope, elm, attrs) ->
    elm.text(version);
])
